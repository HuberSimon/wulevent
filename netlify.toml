[build]
  command   = "npm run build"
  publish   = "dist"
  functions = "netlify/functions"

[dev]
  # lokaler Dev-Server Port (Vite default)
  targetPort = 5173
  port       = 8888
  autoLaunch = false